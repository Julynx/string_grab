import setuptools

setuptools.setup(
    name="grab",
    version="0.0.1",
    author="Julio Cabria",
    description=("Extract one or multiple substrings"
                 "between two start and end strings."),
    license="GPLv2",
    packages=["grab"])
